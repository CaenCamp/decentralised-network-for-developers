import MediaIcon from '@material-ui/icons/AttachFile';
import { MediaList } from './List';
import { MediaCreate } from './Create';
import { MediaEdit } from './Edit';

export default {
    list: MediaList,
    create: MediaCreate,
    edit: MediaEdit,
    icon: MediaIcon,
    options: { label: 'Les supports' }
};
